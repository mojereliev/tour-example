include ../../node_modules/bemto.pug/bemto

+b.page(
  id="app",
  :class="[" +
  "`page_ui_${app.uiColor}`," +
  "`page_platform_${app.isMobilePlatform ? 'mobile' : 'desktop'}`" +
  "]"
)
  transition(
    appear,
    @enter="fadeIn"
  )
    +e.wrapper(v-show="app.isReady")
      headerModule

      +e.container
        mainNav

        transition(
          @enter="fadeIn",
          @leave="fadeOut"
        )
          +e.inner
            nuxt

        footerModule
